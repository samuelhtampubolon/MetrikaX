{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The whole permission set of the application. Nothing here reaches the network, the shell, the clipboard or the file system beyond the two files the person chooses in a dialog.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-set-title",
    "dialog:allow-open",
    "dialog:allow-save",
    "sql:default",
    "sql:allow-load",
    "sql:allow-execute",
    "sql:allow-select",
    "sql:allow-close"
  ]
}
